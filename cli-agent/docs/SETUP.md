# Setup

1. Python 3.11+ kurulu olmalı.
2. `python3 -m venv venv`
3. `source venv/bin/activate`
4. `pip install requests tiktoken rich typer pyyaml python-dotenv`
5. `.env.example` dosyasını `.env` olarak kopyalayıp API key gir.
