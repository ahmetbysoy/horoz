class SessionManager:
    def save(self, session_id: str, data: dict) -> None:
        print(f"[Session] save TODO: {session_id}")

    def load(self, session_id: str) -> dict:
        print(f"[Session] load TODO: {session_id}")
        return {}
